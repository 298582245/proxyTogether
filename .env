# Docker Compose 环境变量配置
# 复制此文件为 .env 并修改相应配置

# MySQL root 密码
DB_ROOT_PASSWORD=root123456

# MySQL 对外端口 (可选，默认3306)
MYSQL_PORT=3306

# Redis 对外端口 (可选，默认6379)
REDIS_PORT=6379

# Web 服务对外端口 (可选，默认80)
WEB_PORT=80
